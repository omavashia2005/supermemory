# Installation record

Required command (Codex selected):

```sh
npx skills add typesafe-ai/skills --skill typesafe-ai --agent codex --yes
```

On 2026-09-17 it failed with `403 Forbidden - GET https://registry.npmjs.org/skills` due to the execution environment's network policy. The TypeSafe documentation URLs and GitHub source were likewise blocked. No upstream skill contents or license could be truthfully persisted. Re-run the exact command with network access; do not use this placeholder for live TypeSafe work.
